#include "Calculations.h"

int main (int argc, const char *argv[])
{
    printf ("Square equation solver\n");
    printf ("a*x^2 + b*x + c = 0\n");

    SwitchTestOrCalc(argc, argv);

    return 0;
}
